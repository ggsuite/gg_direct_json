name: gg_direct_json
description: gg_direct_json allows easy JSON manipulation via a/b/c paths for read/write.
version: 2.1.0
repository: https://github.com/inlavigo/gg_direct_json.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
